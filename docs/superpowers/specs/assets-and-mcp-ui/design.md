# Explore Draft — 资产/MCP UI

> 草稿。契约见 `openspec/changes/assets-and-mcp-ui/`。

PRD §10 + §14 两个模块缺完整页面。本 change 一次性补齐 UI 骨架。Neo4j / RAGFlow 真接入按 Q3=c 推迟，用 mock 数据呈现，保证 UI 骨架到位后未来替换成本低。

选方案 B（两个小 change 一组）：G5 资产详情 + G7 MCP 扩展合并为单一 change 执行，节奏更快且相关性高（都是"数据接入 + 展示"）。
